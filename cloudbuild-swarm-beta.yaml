steps:
# This step builds the container image.
- name: 'gcr.io/cloud-builders/docker'
  id: Build
  args:
  - 'build'
  - '-f'
  - 'Dockerfile.swarm-beta'
  - '-t'
  - 'asia-south1-docker.pkg.dev/likeminds-nonprod-prj-24e1/github.com/nateshr/likeminds-swarm/swarm-beta:$SHORT_SHA'
  - '.'

# This step pushes the image to Artifact Registry
# The PROJECT_ID and SHORT_SHA variables are automatically
# replaced by Cloud Build.
- name: 'gcr.io/cloud-builders/docker'
  id: Push
  args:
  - 'push'
  - 'asia-south1-docker.pkg.dev/likeminds-nonprod-prj-24e1/github.com/nateshr/likeminds-swarm/swarm-beta:$SHORT_SHA'

# generate the manifest
- name: 'gcr.io/cloud-builders/gcloud'
  id: Generate manifest
  entrypoint: /bin/sh
  args:
  - '-c'
  - |
     sed 's/swarm-beta:.*/swarm-beta:${SHORT_SHA}/g' deployment-swarm-beta.yaml

# apply the manifest
- name: 'gcr.io/cloud-builders/kubectl'
  id: Deploy
  args:
  - 'apply'
  - '-f'
  - 'deployment-swarm-beta.yaml'
  - '-n'
  - 'app-deploy'
  env:
  - 'CLOUDSDK_COMPUTE_REGION=asia-south1'
  - 'CLOUDSDK_CONTAINER_CLUSTER=likeminds-nonprod-autopilot-cluster'
  